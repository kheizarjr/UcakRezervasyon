@model UcakRezervasyon.Models.ucus

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>ucus</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="ucakId" class="control-label"></label>
                <select asp-for="ucakId" class ="form-control" asp-items="ViewBag.ucakId"></select>
            </div>
            <div class="form-group">
                <label asp-for="guzergahId" class="control-label"></label>
                <select asp-for="guzergahId" class ="form-control" asp-items="ViewBag.guzergahId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
